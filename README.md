# Constraint Foundry

Deterministic, spec-locked agent-driven build system.

## Goals
- Build software via atomic prompts
- Enforce lint and coverage gates
- Prevent spec mutation
- Provide deterministic retry loop

## Quality Gates
- pylint >= 9.5
- coverage >= 90%
